# CODING_STANDARD

> เอกสารนี้เป็น placeholder สำหรับมาตรฐานโค้ดเท่านั้น งานรอบนี้ไม่ได้แก้โค้ด

## Source notes from current docs
- Do not store API tokens in repo files.
- Run build before deploy.
- Update version badge for visible deploys.
- Attendance check-in/check-out and payroll must be tested before deploy.

## Missing
- ต้องตรวจสอบเพิ่มเติม: lint/test policy ที่เป็น current source of truth
- ต้องตรวจสอบเพิ่มเติม: branching, commit message, PR review standard
